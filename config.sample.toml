debug = false

[server]
host = "127.0.0.1"
port = 5104
secret_key = ""
ssl_keyfile = "./content/ssl/key.pem"
ssl_certfile = "./content/ssl/cert.pem"

[database]
type = "sqlite"

# 如果使用 SQLite 数据库，指定数据库文件名
file = "app.db"

# 如果使用 MySQL 数据库，指定连接信息
host = "localhost"
port = 3306
username = ""
password = ""
# 如果使用 MySQL 数据库，指定数据库名称
db_name = "app_db"
# 如果使用 MySQL 数据库，指定字符集
charset = "utf8mb4"
options = "" # Not implemented yet